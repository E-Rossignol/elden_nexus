name: elden_nexus
description: "An app to help you track your found items on elden ring"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons fonts to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6
  firebase_core: ^3.2.0
  firebase_auth: ^5.1.1
  path: ^1.9.0
  provider: ^6.1.2
  get: ^4.6.6
  shared_preferences: ^2.2.3
  random_string: ^2.3.1
  cloud_firestore: ^5.1.0
  url_launcher: ^6.3.0
  photo_view: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons fonts is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - lib/constants/images/main_game_weapons/axe/
    - lib/constants/images/main_game_weapons/claw/
    - lib/constants/images/main_game_weapons/colossal_sword/
    - lib/constants/images/main_game_weapons/colossal_weapon/
    - lib/constants/images/main_game_weapons/curved_greatsword/
    - lib/constants/images/main_game_weapons/curved_sword/
    - lib/constants/images/main_game_weapons/dagger/
    - lib/constants/images/main_game_weapons/fist/
    - lib/constants/images/main_game_weapons/flail/
    - lib/constants/images/main_game_weapons/great_hammer/
    - lib/constants/images/main_game_weapons/great_spear/
    - lib/constants/images/main_game_weapons/great_axe/
    - lib/constants/images/main_game_weapons/great_sword/
    - lib/constants/images/main_game_weapons/halberd/
    - lib/constants/images/main_game_weapons/hammer/
    - lib/constants/images/main_game_weapons/heavy_thrusting_sword/
    - lib/constants/images/main_game_weapons/katana/
    - lib/constants/images/main_game_weapons/reaper/
    - lib/constants/images/main_game_weapons/spear/
    - lib/constants/images/main_game_weapons/straight_sword/
    - lib/constants/images/main_game_weapons/thrusting_sword/
    - lib/constants/images/main_game_weapons/twinblade/
    - lib/constants/images/main_game_weapons/whip/
    - lib/constants/images/dlc_weapons/
    - lib/constants/icons/
    - lib/constants/images/status_effects/
    - lib/constants/images/ash_of_war/
    - lib/constants/images/ash_of_war/dlc/
  fonts:
    - family: Mantinia
      fonts:
        - asset: lib/constants/fonts/Mantinia/Mantinia_Regular.otf
    - family: GeoSansLight
      fonts:
        - asset: lib/constants/fonts/GeoSansLight/GeoSansLight.ttf
    - family: Chiralla
      fonts:
        - asset: lib/constants/fonts/Chiralla/coolvetica_rg.otf
        - asset: lib/constants/fonts/Chiralla/coolvetica_rg_it.otf
          style: italic

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the fonts family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the fonts. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
